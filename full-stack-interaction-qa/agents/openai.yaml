interface:
  display_name: "Full-Stack Interaction QA"
  short_description: "Correlate backend-dependent interactions with HTTP/DB/trace truth"
  default_prompt: "Use $full-stack-interaction-qa only when HTTP/index/backend lanes exist; correlate this backend-dependent interaction with traces and durable state. Do not pick for UI-only search or rendered UI alone."
