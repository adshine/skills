interface:
  display_name: "Measured Visual QA"
  short_description: "Measure settled 2D layout geometry with evidence"
  default_prompt: "Use $measured-visual-qa to capture, measure, annotate, and verify this settled layout or geometry defect."
